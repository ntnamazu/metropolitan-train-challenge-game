services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env.development
    environment:
      - NODE_ENV=development
      - VITE_ENV=development
    command: npm run dev -- --host 0.0.0.0
    stdin_open: true
    tty: true
